#include <bits/stdc++.h>
using namespace std;
void solve() {
    int64_t a, b;
    cin >> a >> b; 
    cout << (a + b - 1) / b << '\n';
}
int main() {
    ios::sync_with_stdio(0);
    cin.tie(nullptr);
    solve();
    return 0;
}

